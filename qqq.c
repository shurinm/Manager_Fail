#include<stdio.h>
#include<unistd.h>
#include<sys/types.h>

	int main()
{
	int pid;
	pid = fork();
	if(pid == 0)
	{
		pid = fork()
	}
	else 
		shpid = fork();
	if(shpid == 0)
	{
		shpid  = fork()
		fork();
	}
	else 
		exit(0):
}
